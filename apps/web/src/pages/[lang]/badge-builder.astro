---
import astroConfig from '../../../astro.config.mjs'
import Page from '@/pages/badge-builder.astro'

export function getStaticPaths() {
	return astroConfig.i18n?.locales.map((lang) => ({
		params: { lang },
	}))
}
---

<Page />
